{
  "preprocess_lyrics": {
    "description": "是否预处理歌词以提高匹配准确率",
    "type": "bool",
    "hint": "开启后会对歌词进行预处理，如去除标点、统一大小写等，以提高匹配准确率",
    "default": true
  },
  "match_threshold": {
    "description": "歌词匹配阈值",
    "type": "float",
    "hint": "匹配阈值越高，匹配越精确，但可能导致部分歌词无法匹配。范围: 0.1-1.0",
    "default": 0.8
  }
}
